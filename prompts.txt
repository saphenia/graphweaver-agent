load business_rules.yaml from file then execute all rules then
import this lineage into the Neo4j graph then
Find hub tables that are central to your data ecosystem then
First discover FK relationships in your database2. Add those FKs to the graph 3. Then analyze centrality with both FK and lineage data then
connect all datasets to their matching tables in the graph then
generate text embeddings then
generate kg embeddings then
find similar columns to orders.customer_id then
predict fks from embeddings then
sync graph to RDF
